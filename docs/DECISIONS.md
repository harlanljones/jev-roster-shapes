# Decisions and unresolved dependencies

This register distinguishes implementation defaults from external commitments. Defaults below are selected for an immediately buildable synthetic prototype. They may be revised with a documented reason and updated contracts/fixtures. Unresolved pilot decisions block only their dependent work.

## Adopted prototype defaults

| ID | Decision | Reason and consequence |
| --- | --- | --- |
| D-01 | Build a local synthetic prototype first | No team data, partnership, staffing, or deployment has been established. Synthetic work can proceed now. |
| D-02 | Use full-game templates and one player per batting/defensive-role slot | Makes simultaneous assignments and opportunity conservation explicit. Substitutions and pitcher batting require another model version. |
| D-03 | Represent defense in integer outs, PA as explicit integer projected counts | Prevents innings-notation errors and hidden opportunity creation. Fractional expected PA require a future contract revision. |
| D-04 | Use one overall/split mode and metric basis per comparison | Prevents incompatible deltas. Starter hand is metadata; exposure is separately supplied. |
| D-05 | Make taxonomy/classification optional | A readable comparison is independently valuable; a failed classification experiment must not block it. Initial profiles may be unclassified. |
| D-06 | Use explicit local saves and portable versioned JSON bundles | Supports drafts and replay with a small prototype. Browser storage is not a team deployment or backup plan. |
| D-07 | Default scaffold to TypeScript, React, and Vite | **Superseded by D-13 on 2026-09-19.** Fits a browser prototype with shared types and isolated pure calculations. RS-01 must record versions, package manager, tests, and actual verified commands; these dependencies are not installed yet. |
| D-08 | Keep calculations, persistence, UI, and provider adapters separate | Enables bounded agent ownership and numerical tests independent of rendering/network state. |
| D-09 | Select coverage-only or coverage-and-offense review scope explicitly | Missing projections do not invalidate known coverage; they do prohibit unsupported full value comparisons. |
| D-10 | Show neutral/unclassified profiles until a rubric and color scale are defined | Avoids inventing baseball judgments for a visually polished demo. Use real quantities and labeled synthetic values. |
| D-43 | **Shape Case redesign: the roster is a fitted case, and the diagrams are a labeled display layer over actual 2026 production.** Harlan asked for the app to be redesigned around the Red Sox Shape Case concept artifact, with its philosophy and content taking priority, and chose the "Case + Magnet Board" mix of the mockups: the landing page (`/`, `/scenario/[slug]`) leads with the case, and the workspace opens on side-by-side capacity bins. Four diagrams are ported from the artifact into `src/lib/app/shape-case.ts` (pure model) and `src/lib/app/diagrams/` (Svelte): **the case** (each lineup slot is a foam cutout cut to the shape it asks for; pieces are rubric shapes; visible foam is friction; the bench tray shows empty cutouts for positions nobody on the bench or the DH covers), **the interaction map** (shared positions, tested swaps from the storylines, platoon complements, DH lane), **what's left off** (the nine slots packed by gravity into a bin whose lid is the pool's tightest fit), and **slot by slot** (bars by slot and pitcher hand). Equal-area geometry lives in `src/lib/shapes/geometry.ts`, so shape never changes size.<br>**Sizing and color.** Piece area is proportional to actual 2026 runs = actual 2026 PA × the bundle's observed R/PA; each piece is split into a vs-LHP half and a vs-RHP half, scaled by that split's OPS against the player's own PA-weighted OPS (clamped 0.6–1.4) and colored Savant-style against the league OPS for that hand (.719 / .727, saturating at ±.150). Split run estimates (observed R/PA × split OPS / own OPS) drive the bin halves, the bar fills, and the tightest-fit search, and are labeled a judgment layer everywhere. A player without split evidence or without a rate has no run total; lineup totals, bin runs, and bar totals are suppressed rather than zero-filled.<br>**Split evidence is a display-only snapshot.** `src/lib/app/split-evidence.ts` records 2026 statSplits PA and OPS (vs LHP / vs RHP) for 14 of the 15 pool players, fetched 2026-09-24 from the MLB Stats API, plus league lines summed from the team rows the API returned (25 of 30 teams per side, so close estimates). It is outside every bundle, digest, and engine input; the pinned 10-game engine totals are unchanged and remain the primary number in the workspace cards, labeled separately from the case's actual-2026 totals. D-42 still holds for the engine: OPS is not an additive run measure and the bundles' split rates stay null.<br>**Rubric v2 (Star).** Harlan's definition replaces v1's: a Star is a quirky or tough fit, not a star player. The recorded rule is a platoon gap of at least .200 OPS with 50+ PA on each side. Kiner-Falefa (.445 / .708) and Monasterio (.889 / .642) become Stars; Durbin and Contreras (v1 Stars for elite production, no platoon quirk) become Squares. Cutout asks and fit grades (snug / fits / loose) are a proposed judgment layer. Reversible without touching any bundle or result.<br>**Look.** One world across the app (DESIGN.md): a cool steel board with a faint grid and white panels, the dark foam case as the object, Saira Stencil One display type with IBM Plex Sans and Mono (self-hosted via Fontsource, no third-party font requests). The old roster graphic, live roster render, and hand-written depth snapshot are removed; the workspace keeps every editing, evidence, save/import/export, and review path. | UI owner (`src/lib/app`, `src/lib/shapes`, `src/lib/ui` styling) | User directive (Harlan, 2026-09-24): redesign around the Shape Case, artifact philosophy and content first, diagrams derived rather than copied, focus on the diagrams; mix of mockups A and C chosen. The artifact already sized by actual PA and used split OPS; keeping that as a display layer preserves SPEC §6 (geometry never creates value) and the pinned engine results. | Feeding split OPS into the engine; replacing engine totals with actual-2026 totals; keeping rubric v1 Stars; loading Google Fonts at runtime; a dark-only theme across the whole app |

## Implementation decisions, 2026-09-19

The user adopted these in an interactive design review before RS-01. Each entry is adopted but not yet verified. The owning task records validation evidence in the [roadmap](../ROADMAP.md) implementation handoff when it closes. The research behind D-13 to D-16 is in [research/2026-09-19-frontend-stack.md](research/2026-09-19-frontend-stack.md).

### Process

| ID | Decision | Task | Rationale and trade-off | Rejected alternatives |
| --- | --- | --- | --- | --- |
| D-11 | Build RS-01 through RS-07 in this cycle. Stop for user review at each wave gate: after RS-02, after RS-03/04/05, and after RS-06. RS-07 ends with its audit report. RS-08 to RS-10 stay out of scope because they need people, permissions, or external decisions. | Integrator | Reviews fall where later work starts depending on frozen outputs. | Stopping only after RS-02 or only when blocked; RS-01 and RS-02 only |
| D-12 | `main` is the default branch. Its root commit holds the unchanged handoff documents, and implementation happens on `feat/prototype`. Commits happen only when the user asks; while the permission check blocks the agent from committing, the agent lists each task's files at each review gate for the user to commit. RS-03, RS-04, and RS-05 are built in parallel by Sonnet 5 subagents, each owning only its component. Worktrees need a commit to start from, so the isolation method is settled at the RS-02 gate. The integrator merges the components in dependency order and reruns cross-component checks. | Integrator | The session's permission check blocks agent commits, so the user controls when commits happen. Parallel work is faster but adds merge and consistency risk, which the frozen RS-02 contract limits. | Keeping `master`; the agent committing per task or at gates; building wave 3 sequentially; Opus 5 or mixed models for subagents |

### Stack and tooling

| ID | Decision | Task | Rationale and trade-off | Rejected alternatives |
| --- | --- | --- | --- | --- |
| D-13 | Build the UI with Svelte 5.57 on SvelteKit 2.70, as a static single-page app (adapter-static, no server rendering) on Vite 8.3. Supersedes D-07's React default. | RS-01 | User choice after the stack research. Stable and concise, and single-page mode fits local-only use while keeping a hosted adapter available for O-05. Costs: svelte-check supports TypeScript 6 at most (see D-14), and SvelteKit's `$app` modules need test doubles in component tests. | React 19.3 (the research's lead candidate); Solid 2.0 RC; Ripple 0.4; Svelte without SvelteKit; SvelteKit 3 pre-release |
| D-14 | Use TypeScript 6.0.3 only. Lint with ESLint 10 + typescript-eslint 8.70 (type-aware) + eslint-plugin-svelte 3.23. Format with Prettier 3.9 + prettier-plugin-svelte 4.1. | RS-01 | One compiler covers `.svelte` and `.ts` files, and ESLint is the only linter that checks Svelte templates with type information. Revisit TypeScript 7 once svelte-check supports it. | TypeScript 6 and 7 side by side; Oxlint, alone or with ESLint; Biome (its Svelte support is experimental); no linter or formatter |
| D-15 | Bun 1.4.2 installs packages and runs repository scripts. Node 26.7.0 runs Vite, Vitest, svelte-check, and ESLint. uv 0.12.17 handles any Python. `mise.toml` pins the three tools; `package.json` pins exact dependency versions and names `bun@1.4.2` as package manager; `bun.lock` is committed. | RS-01 | The user directed Bun and uv, at their newest versions, for all package management. The tools run on their documented runtime. | Running the tools on Bun; npm or pnpm; caret version ranges |
| D-16 | A Vitest 5 Node project tests contracts, storylines, engine, and persistence. UI components are tested in Vitest Browser Mode, in Playwright's Chromium, with vitest-browser-svelte. Playwright Test with axe-core runs end-to-end journeys and the RS-07 latency measurement. RS-07's reference setup is this Apple M1 Mac, Chromium only, with a production build. | RS-01, RS-05, RS-07 | Keyboard, focus, and dialog behavior need a real browser; pure logic stays fast in Node. Other browsers remain untested, and RS-07 must report that. | jsdom component tests; running journeys in Vitest only; measuring in all three browsers |
| D-17 | Code lives in `src/lib/{contracts,storylines,shapes,engine,persistence,ui}`, plus `classification` if RS-09 starts. The integrator owns `src/routes`, `src/lib/app`, and root configuration. Tests live in `tests/<component>`, `tests/integration`, and `tests/e2e`. Imports use `$lib`. eslint-plugin-boundaries 7.2 enforces the allowed dependencies between components. | RS-01 freezes; all tasks | Idiomatic SvelteKit that keeps the roadmap's ownership map. Boundary violations appear in the editor and in lint. | Tests beside source; top-level `src/<component>` via aliases; a custom boundary test; dependency-cruiser |

### Runtime architecture

| ID | Decision | Task | Rationale and trade-off | Rejected alternatives |
| --- | --- | --- | --- | --- |
| D-18 | The engine runs in a Web Worker behind an asynchronous interface. Each result carries its input revision, so late results from older revisions are discarded. | RS-03 (pure engine), RS-06 (worker wiring) | Makes the `Updating` state and I-06 real, and keeps input responsive on larger workloads. Worker messaging adds about a millisecond to each edit. | Main thread behind an async wrapper |
| D-19 | Local persistence uses IndexedDB behind the repository interface. | RS-04 | Asynchronous, with a large quota and room for structured revision history; I-05 failures can be simulated. | localStorage; Origin Private File System |
| D-20 | Pointer drag-and-drop ships in v1 as an enhancement. It uses the same preview-and-commit path as the required keyboard selectors and Move/Swap controls. | RS-05 | User choice. Every operation stays available by keyboard; drag-and-drop adds UI and test work. | Selectors and Move/Swap only |
| D-21 | Decide these at the design checkpoint before RS-05: the visual direction (the user picks from one or two mockups), the styling approach, the accessible component library, chart rendering, and how drag-and-drop is implemented. | RS-05 with the user | They depend on the chosen visual direction. | Deciding them now, without mockups |

### Data contract (RS-02)

| ID | Decision | Task | Rationale and trade-off | Rejected alternatives |
| --- | --- | --- | --- | --- |
| D-22 | The v1 validator is executable TypeScript built with Zod 4.6. No JSON Schema file is published until something outside the app needs one. The input digest is SHA-256 computed synchronously with @noble/hashes 2.4. | RS-02 | Zod rejects unknown fields, reports exact issue paths, and infers types. A synchronous digest keeps the engine API synchronous in every runtime. | Valibot; ArkType; a hand-written validator; generated or authoritative JSON Schema; Web Crypto |
| D-23 | **Import rejects the whole bundle for:** shape or type errors; unknown or missing fields; unsupported versions; duplicate IDs or keys; unresolved references; units other than `runs_per_PA`; malformed template or allocation structure; anything other than exactly one cap record per member; duplicate or non-member cost rows; half-filled acknowledgments; and acknowledgments whose revision differs from the scenario's. **Everything else imports as a draft for the engine to flag:** membership mismatches, assigned non-members, repeated players, ineligible positions, caps, empty slots, rates, exposure, and constraint results. A cost row whose currency or period differs from an enabled budget makes the cost check `unknown`. | RS-02, RS-03 | Broken references can't be imported (DATA_CONTRACT §4), but domain violations must stay openable as drafts (C-03, C-05, C-08, C-25). | Rejecting more (conflicts with ACCEPTANCE); rejecting less; treating stale acknowledgments as absent |
| D-24 | Add issue codes `DUPLICATE_ID`, `ACKNOWLEDGMENT_REQUIRED`, `NUMERIC_OVERFLOW`, and `REPLAY_MISMATCH`. Issue paths are RFC 6901 JSON Pointers, sorted segment by segment (array indices as numbers), then by code. Offense and constraint reasons use the Issue shape, and `issues` lists every diagnostic, readiness blockers included. The result validator enforces the consistency rules in DATA_CONTRACT §5. Readiness for the whole comparison (a saved revision; baseline, A, and B all present) belongs to RS-06, which adds its own codes. | RS-02 | Every diagnostic links to a field, and RS-03 can check every engine output with the same validator. | Reusing existing codes; dotted paths; checking result shapes only; listing blockers only under `readiness` |
| D-25 | Checks stricter than the contract's letter: IDs are visible ASCII without whitespace. Names, labels, and titles are non-empty. `sourceIds` are non-empty. Decimals match `-?(0\|[1-9]\d*)(\.\d{1,6})?`. Currency is three capital letters. Timestamps are valid UTC times ending in `Z`. Each projection's `sourceId` equals its metric definition's, otherwise `METRIC_MISMATCH`. Zero-game templates have zero PA. | RS-02 | Catches ambiguous or unsourced data at the import boundary. | The contract's letter only |
| D-26 | Fixtures form a typed registry of variations built from the golden bundle. Each fixture has a stable ID, its ACCEPTANCE case, its expected import outcome, and a pinned input digest. Edits to shared inputs bump revisions as the contract requires, and each fixture has its own `bundleId`. RS-02 also publishes hand-derived expected results for the golden fixture only, labeled `calculationVersion: "hand-derived-acceptance-v1"`. | RS-02 | Any content change fails the tests. The UI can render real-shaped results before the engine exists, and RS-03 is tested against expectations it didn't write. | One JSON file per case; expected results for every fixture; inputs only |
| D-27 | How ambiguous acceptance cases are read: <br>• C-03: in the baseline's vs-left-starter template. <br>• C-05: in Candidate A, `p-a` moves to catcher and `p-2b` returns to second base. <br>• C-06: the golden fixture itself is the 40-PA boundary case for Candidate A's `p-a`. <br>• C-11: the shared 2B slot's 20 L / 20 R total splits 8/8 in the vs-left template and 12/12 in the vs-right template. <br>• C-12: builds on C-09. <br>• C-13: uses `p-dh`; the calculable variant moves DH exposure to right-handed pitching. <br>• C-20b: the baseline's costs use test currency `XTS` for period `pilot-horizon`. <br>• C-23: two fixtures, with defensive-out caps at 270 and at 280. <br>• C-24: one fixture with an out-of-range input integer (rejected at import) and one with an out-of-range product (`NUMERIC_OVERFLOW`). <br>• C-25: removes `p-reserve` from the baseline. | RS-02 | Makes the fixed test inputs explicit. | Reading C-05 literally (both duplicate and ineligible) |
| D-28 | The validator accepts `synthetic`, `public`, and `restricted` bundles. The app opens only `synthetic` until RS-08 records permission, and explains why others are blocked. | RS-02, RS-06 | Validation stays a pure contract check, and the policy sits where permission will eventually be granted. | Rejecting non-synthetic bundles in the validator; opening anything behind a warning banner |
| D-29 | Known v1 limitation: acknowledgments are bound to scenario revision numbers. A bundle edited by hand without bumping its revision keeps its acknowledgment. Bundles with cached results would still fail replay, which blocks readiness. Revisit before analysts share bundles. | RS-04, RS-06 | Avoids a schema change for a risk the app's own editor doesn't create. | Adding a content digest to `review` |
| D-30 | The RS-01 app shell shows the title, `Synthetic demo data`, an explicit empty state, and a check that the browser build validates the golden fixture and computes its digest. No product UI or visual design. | RS-01 | Proves the contract code runs in the browser build without taking over RS-05's work. | A blank page; starting the workspace layout |
| D-31 | Close the post-RS-07 UI gaps inside the frozen interfaces: a per-template keyboard-operable **Move** control relocates one slot's player to an **Unassigned** slot (the source becomes `Unassigned`); moving onto an occupied slot is blocked client-side with a pointer to Swap. Swap and Move forms show a **live single-commit preview** (slot order, role, PA, affected players) rather than a two-step confirm; the engine remains the validator and the existing Swap e2e keeps passing. Closing the evidence drawer returns keyboard focus to its trigger (WORKFLOWS §8). | UI owner (`src/lib/ui`, `src/lib/app`) | Move covers the WORKFLOWS §6 Move/Swap keyboard parity without changing bundle, engine, or persistence contracts; live preview satisfies "show both destination assignments before applying" with the least flow churn; focus-return is a small a11y repair. | Two-step preview-and-confirm for Swap/Move; Move that displaces an occupant; auto-picking a destination slot |
| D-32 | Repair the two full-axe findings without changing contracts or copy meaning: darken `--muted` `#716f67`→`#5f5d56` and `--rust` `#a84f32`→`#9c4a2e` (same hues; ≥4.9:1 on `--paper-deep`, higher on lighter surfaces), and rename the inner AssumptionsPanel heading to "Shared planning context" so the outer "Assumptions" region landmark keeps a unique name. | UI owner | All 25 color-contrast nodes shared one root cause (muted/rust text on `--paper-deep`); a two-variable palette shift fixes every panel at once. The duplicate region name came from nested "Assumptions" landmarks. | Per-node color overrides; leaving the full audit pinned; a full visual redesign |
| D-33 | Implement drag-and-drop with native HTML5 DnD on the allocation table rows (settles the open D-21 implementation point). Dragging an assigned row onto an occupied slot in the same template commits a Swap; onto an Unassigned slot commits a Move — both through the existing `onAssignmentSwap`/`onAssignmentMove` callbacks, so the engine stays the validator. A live drop preview names both slots; cross-template and same-slot drops are ignored. Drag is pointer-only: every operation remains available through selectors and Swap/Move forms by keyboard, and no calculation logic moves into the UI. | UI owner (`src/lib/ui`) | Native DnD needs no dependency and reuses the tested commit path; swap/move-from-drop mirrors the form semantics users already learned. | Pointer Events / custom drag layer; touch support (out of scope — pointer mouse/trackpad only); two-step drop confirm |
| D-34 | Spike a public-data adapter without touching the app open path (D-28 holds: the app still opens only `synthetic` until RS-08 records permission). `spikes/mlb-public/` holds a fetch script plus one static `public`-class bundle built from completed-2025 Red Sox data: MLB Stats API for identity/bats/primary-position/roster/observed stats, observed R/PA as the single `runs_per_PA` metric in `overall` mode, splits explicitly null, eligibility from primary position only with a recorded limitation (Lahman Fielding is the follow-up for multi-position eligibility). No FanGraphs content (needs a membership export the user has not provided) and no network calls at bundle load. | Integrator (spike) | Proves the contract accepts real-shaped public inputs through the frozen validator and engine before any RS-08 commitment; static output keeps the "no network resolution to load a bundle" guarantee. | Live-fetching adapter in the app; scraping FanGraphs; treating observed totals as projections without a label; multi-position eligibility in the spike |
| D-35 | Source multi-position eligibility from the same MLB Stats API `fielding` season splits (one row per position stint; aggregated across teams) instead of the planned Lahman download: a position is eligible at **≥10 games played** in 2025 (spike default, evaluator decision TBD). DH rows are excluded (DH is not an eligibility entry). The slot-plan guard now asserts each field assignment against computed eligibility rather than primary position. | Integrator (spike) | Same fetch date and provenance as the rest of the spike, no 179MB download, and the threshold is explicit and auditable in the bundle; Lahman stays the independent cross-check option. | Lahman Fielding download in the spike; GS-based or lower thresholds; carrying DH as eligibility |
| D-36 | Implement the missing D-28 policy plus the user-requested public demo path: the importer pre-reads `dataClass` before touching storage. `restricted` bundles are hard-blocked with an explanation naming the RS-08 permission record; the open comparison stays intact. `public` bundles open only after an explicit per-bundle-ID acknowledgment ("observed public values, not team-approved projections"), remembered for the session; declining keeps the current comparison. Shell/hero labels (badge, eyebrow, lede, title) render from the open bundle's `dataClass` so a public comparison never claims to be synthetic. | UI owner (`src/lib/app`) | Enforces D-28's letter for restricted while giving the requested public demo a front door with an audit-visible acknowledgment; labels stay truthful in every state. RS-08 is still required for team data and any decision use. | Silent public open; query-param bypass; acknowledgment that persists across sessions |
| D-37 | The web servers (dev/build/preview serve one SvelteKit app) default to the public spike bundle instead of the synthetic fixture. The app first paints the synthetic fixture with an immediate per-session public acknowledgment prompt (D-36, extended to startup); confirming opens `redsox-observed-2025.json`, declining keeps synthetic. Saved drafts restore by ID (public first, then synthetic) so prior work survives. The runtime copy lives at `src/lib/fixtures/redsox-observed-2025.json`, pinned equal to the spike output by a fixture test; a spike rebuild must be consciously re-copied, reviewed, and re-tested. Copy and evidence strings stay data-aware (observed R/PA, placeholder caps, no splits, one candidate). | UI owner (`src/lib/app`, `src/lib/fixtures`, `src/lib/ui` evidence copy) + integrator (tests/docs) | User request for real data on the web servers; the only permitted real data is the public spike (no RS-08 record exists, so restricted/team data stays blocked). One-click acknowledgment keeps the D-36 permission pattern for the new default path. | Auto-opening public without acknowledgment; inventing a second public candidate or projections to match the synthetic shape; silent synthetic removal (synthetic stays importable and remains the contract golden fixture) |
| D-38 | **Deliberate contract revision (user directive 2026-09-21): remove the RS-02 synthetic fixtures and the 2025 public spike from the app and test surface; the UI starts from five 2026 public storyline bundles.** Deleted: `src/lib/fixtures/` (golden `comparison-v1.json`, `redsox-observed-2025.json`, registry with pinned digests and hand-derived `hand-derived-acceptance-v1` expectations), `spikes/mlb-public/redsox-observed-2025.json`, and every test pinning them. Replacement executable examples: five `public`-class v1 bundles in `src/lib/storylines/` built from 2026 observed MLB Stats API totals (D-40), each baseline + two candidates with hand-verified expectations asserted in `tests/storylines/registry.test.ts`. Retained read-only as historical audit trail (not loaded by app or tests): `docs/examples/comparison-v1.json`. **Unchanged:** schema `1.0`, validator semantics, `deterministic-engine-v1`, persistence versioning, ACCEPTANCE calculation cases C-02–C-26 (re-targeted at storyline bundles in the rewritten engine tests). ACCEPTANCE §1's golden table is superseded by the storyline expectation table below in D-40. Old bundle IDs (`synthetic-comparison-v1`, `mlbam-bos-observed-2025-spike`) no longer resolve; saved drafts under those IDs are not migrated — export-then-import of v1 bundles still works through the unchanged validator. Post-revision ID mapping for the re-targeted tests: golden `baseline`/`candidate-a`/`candidate-b` → storyline `base`/`cand-a`/`cand-b`; `vs-left-starter`/`vs-right-starter` → `bos26-vs-left`/`bos26-vs-right`; `p-a` (40-PA boundary/cap case) → Anthony (`mlbam-701350`); `p-reserve` (unused-reserve case) → the baseline reserve of the storyline under test. | Integrator + UI owner | The user explicitly chose file deletion over hide-and-keep after being warned that the golden fixture is frozen by ACCEPTANCE §1 and pinned by digest tests. AGENTS.md requires a deliberate contract revision be documented in the same change, which this entry is. | Keeping golden as the UI default; hiding old comparisons while leaving their tests pinning them in place |
| D-39 | **Adopt analyst-labeled 8-shape taxonomy rubric v1** (`docs/SHAPE_TAXONOMY.md`, `src/lib/shapes/taxonomy.ts`) for the 2026 roster graphic. Each of Square, Rectangle, Circle, Pentagon, Octagon, Diamond, Star, Funky has a plain-language definition, positive examples, counterexamples, required inputs, and boundary rules; Triston Casas (0 PA, no eligibility) is Unclassified, preserving SPEC §6's required unclassified state. Labels are **assumption-layer judgments** (analyst-labeled, rubric `shape-rubric-v1`, no evaluator consensus claimed — O-03 stays open) and never enter the calculation identity: geometry cannot create value (C-21 equivalent asserted per storyline bundle). | UI owner | The user explicitly chose full 8-shape labeling over neutral tiles after being warned that SPEC §6/O-03/D-10 keep shapes advisory until evaluator agreement exists. This entry plus the rubric doc satisfy SPEC §6's "document before enabling" prerequisite at analyst-label grade, not evaluator-consensus grade. | Neutral/unclassified tiles only; claiming evaluator agreement or predictive value for shapes |
| D-40 | **Library-first UI.** `/` opens a start screen whose first section is the interactive roster-and-shapes graphic (SVG diamond with eight position lanes + DH, shape glyph per D-39, click/keyboard selection, player detail, full table equivalent), followed by five 2026 storyline cards. This implements the WORKFLOWS §2 Library/start view the prototype was missing. Opening a storyline goes through the D-36 per-bundle public acknowledgment; the workspace keeps save/export/import, evidence drill-down, Swap/Move/keyboard/drag editing, and the restricted-bundle block unchanged. The five storylines (baseline/A/B each, shared 10-game illustrative horizon, overall observed-2026 R/PA, splits null, placeholder caps, costs disabled):<br>• `mlbam-bos-2026-power-vacuum` — who carries the lineup without Devers/Bregman: baseline Yoshida DH (39.13172 runs / 360 PA), A Anthony everyday (38.3194, Δ −0.81232), B Casas DH hope (offense unavailable — MISSING_RATE, coverage still comparable).<br>• `mlbam-bos-2026-outfield-logjam` — four gloves + DH: baseline Anthony LF/Duran DH (38.3194), A WBC-hot Yoshida DH/Duran LF (39.13172, Δ +0.81232), B Eaton speed DH (37.46168, Δ −0.85772).<br>• `mlbam-bos-2026-infield-reset` — worst infield to steady: baseline Mayer 2B (38.3194), A IKF everyday 2B (39.95648, Δ +1.63708), B Monasterio utility (39.2414, Δ +0.922).<br>• `mlbam-bos-2026-catcher-split` — Narváez middle ground vs Wong rebound: baseline Narváez C (38.3194), A Wong starts (39.05276, Δ +0.73336), B both catchers/Anthony sits (38.32944, Δ +0.01004).<br>• `mlbam-bos-2026-lefty-hole` — no Refsnyder, no Romy (splits unavailable; overall rates only): baseline Yoshida DH (39.13172), A IKF righty 2B (40.7688, Δ +1.63708), B Monasterio SS/Story sits (39.57752, Δ +0.4458).<br>All scenario totals are 40 PA × Σ observed rates over nine starters; each storyline asserts its hand-derived values in `tests/storylines/registry.test.ts`. | UI owner (`src/lib/app`, `src/lib/storylines`, `src/lib/shapes`) + integrator (tests/docs) | User directive: start with the roster+shapes graphic, remove current comparisons, ground five comparisons in this season's real storylines (Globe/Boston.com/NBC Boston March 2026 previews + Baseball-Reference 40-man: power vacuum, outfield/DH logjam, infield reset, catcher split, vs-LHP hole). Observed-not-projected rates, placeholder caps, no splits, and illustrative horizon are labeled in every bundle. No RS-08 record exists; restricted/team data stays blocked. | Inventing 2026 projections or splits; reusing 2025 rates; a sixth storyline; auto-opening a bundle without acknowledgment |
| D-41 | **Demo deployment: Cloudflare Pages via GitHub Actions.** `.github/workflows/deploy-demo.yml` builds the static SvelteKit app (`bun install --frozen-lockfile`, `bun run check`, `bun run build`) and deploys `build/` with `cloudflare/wrangler-action@v4` (`pages deploy build --project-name jev-roster-shapes`). Pushes to `main` go to production; same-repository pull requests and manual dispatches produce previews, while fork pull requests only build and validate because repository secrets are unavailable. No `wrangler.toml`: Pages direct-upload needs none for this binding-free static site, and the single `/` route needs no SPA fallback. The Pages project is provisioned once by an authorized owner rather than masking `pages project create` errors in CI, so authentication or project failures remain visible. `export const prerender = true` sits alongside `ssr = false` so adapter-static emits `index.html` — the fallback-only build 404'd at `/` on first deploy. Requires repo secrets `CLOUDFLARE_API_TOKEN` (Pages:Edit) and `CLOUDFLARE_ACCOUNT_ID`. Public-only demo content — this does not satisfy the RS-08/RS-10 team-data, access-control, or retention prerequisites (O-05 stays open), and restricted bundles stay hard-blocked in the app regardless of hosting. | Integrator | User request for a hosted demo; Cloudflare Pages fits the existing adapter-static output per the framework guide, and GH Actions keeps the build reproducible in CI instead of Pages' built-in CI. | Pages built-in Git integration; Workers Static Assets + `wrangler.toml`; Vercel/Netlify |
| D-42 | **Rebuild the 2026 lineup workload illustration from observed batting-order PA, keep splits honest, and show one canonical roster board.** A nine-inning 2026 BOS–PIT box score (Aug 14; official MLB lineup and public box score) records 39 PAs across nine starting slots: 5 PAs for orders 1–3, 4 for orders 4–9. Apply that pattern to the shared ten-game horizon (four left-starter context games, six right-starter context games): orders 1–3 get 20/30 PAs per context, orders 4–9 get 16/24. Keep L/R distribution as explicit illustrative exposure assumptions, not observed split projections. Public 2026 split tables ([vs. left](https://www.mlb.com/redsox/stats/?split=vl), [vs. right](https://www.mlb.com/stats/boston-red-sox?split=vr)) provide PA/AB and OPS by pitcher hand, but not the bundle's additive observed runs-per-PA split metric; retain null split rates and suppress split-specific contribution. Render each context lineup side by side and use one shape board per player across contexts; board whitespace remains visual only. Baseline player assignments remain the same across contexts; no substitution is inferred from OPS alone. This supersedes D-40's equal-share PA and derived run expectations; schema and rate source remain unchanged. | UI + data integrator | Box-score PAs by batting order provide a concrete, reviewable workload basis; a pinned stable schema and overall metric remain unchanged. Split OPS is evidence, not interchangeable with additive runs-per-PA; no new inference or conversion is authorized. | Equal 4 PA for every slot; equal 24 PA for every slot in both contexts; inferring L/R exposure from starter hand; treating OPS as run contribution; duplicated headshot/shape boards |

## Open decisions

Owner roles are responsibilities to assign, not named commitments. `TBD` is an unresolved external fact, not permission to fabricate a default for the real pilot.

| ID | Question / required evidence | Role | Blocks | Safe work meanwhile |
| --- | --- | --- | --- | --- |
| O-01 | Which approved projection and eligibility sources, units, rights, and update cadence? | Data/analytics lead | Team ingestion and source mappings | Fixture contracts and synthetic engine |
| O-02 | Which planning horizon, PA methodology, caps, and opponent exposure assumptions reflect the actual decision? | Baseball evaluator | Real scenario conclusions | Explicit synthetic assumptions |
| O-03 | Does the eight-shape taxonomy have usable definitions and agreement? | Evaluator lead | Shape rubric adoption | Neutral profile display and measured allocation |
| O-04 | Is provider processing permitted for each input field; what are retention and access requirements? | Data owner | Proprietary provider requests | Adapter interface or public/synthetic evaluation |
| O-05 | Which environment, identity system, access roles, persistence, retention, and backups support team use? | Engineering/data owner | Shared deployment | Local synthetic prototype |
| O-06 | Who supplies evaluation time and scoring; what sample size and failure policy? | Pilot lead | Outcome study and efficacy claims | Usability rehearsal and instruments |
| O-07 | What constitutes an acceptable classification error, review burden, latency, and cost? | Analytics/model lead | Model adoption | Exploratory results labeled as such |
| O-08 | Which cost period and roster constraints should the first decision actually enforce? | Baseball/analytics lead | Real cost/transaction interpretation | Disabled checks labeled unchecked |

## Public refresh policy

The common public 2026 decision-pool snapshot is refreshed daily by
`.github/workflows/refresh-public-data.yml`. The job runs the existing MLB Stats
API builder against the configured pool and opens a pull request when generated
bundles change. It does not change retrospective event metadata, silently rewrite
the pinned registry, or deploy directly. A reviewer must reconcile changed
eligibility, rates, input digests, and hand-derived storyline expectations before
merging the refresh PR. The builder fails before writing any bundle when a
configured scenario player is absent from the current 40-man roster; it never
silently invents a replacement or leaves a stale player in a refreshed scenario.

## Change protocol

For a material implementation choice, append its ID, status, owner/task, rationale, affected contracts, and validation evidence before dependent work. A contract revision changes the schema/calculation version when old records would otherwise be interpreted differently. Preserve old fixtures and provide an explicit migration plan.

A local UI arrangement or library choice within the frozen interfaces can be resolved by its component owner. Changes to projection meaning, workload arithmetic, readiness, or permissions require the corresponding domain or data owner; continue independent tasks rather than halting the entire prototype.

## Handoff readiness

Development may start with RS-01. The architecture default and synthetic modeling assumptions are defined; no app scaffold, deployment, calibration, or user study is claimed complete. The first development report should establish actual commands and component ownership, then run RS-02 against the supplied golden fixture.
