<script lang="ts">
	import DecisionsPage from '$lib/app/DecisionsPage.svelte';

	// D-46, moved by D-49: the season index — the timeline with today marked,
	// and the five dated decisions — lives at `/decisions`, so `/` can open on
	// the current decision.
</script>

<DecisionsPage />
